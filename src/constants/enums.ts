export enum UserRole {
  ADMIN = 'admin',
  USER = 'user',
  GHOST = 'ghost',
}

export enum MessageType {
  TEXT = 'text',
  IMG = 'img',
}

export enum FriendStatus {
  REQUESTED = 'requested',
  ACCEPTED = 'accepted',
  DECLINED = 'declined',
}

export enum RoomType {
  PUBLIC = 'public',
  FRIEND = 'friend',
  SINGLE = 'single',
}
